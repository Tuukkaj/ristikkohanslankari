{
  "name": "ristikkohanslankari",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm run build && rm -r ../server/public/* && mv dist/* ../server/public/ && cd ../server/ && npm run prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "eslint": "^8.57.0"
  }
}
